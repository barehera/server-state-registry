interface:
  display_name: "Manage React Server State"
  short_description: "Adapt type-safe server state to any React project"
  default_prompt: "Use $manage-react-server-state to inspect this project and create or adapt its server-state feature without forcing a foreign folder structure."
