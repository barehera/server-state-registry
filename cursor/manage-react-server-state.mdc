---
description: Create or adapt type-safe React server-state code to this project's existing structure, backend contracts, transport, validation, authentication, naming, and cache behavior.
globs:
  - "**/server-state/**/*.{ts,tsx}"
  - "**/*query*.{ts,tsx}"
  - "**/*mutation*.{ts,tsx}"
alwaysApply: false
---

Read and follow `@.agents/skills/manage-react-server-state/SKILL.md` before changing remote-state code. Inspect the repository first, load only the references required for the task, and adapt the semantic responsibilities to the project's existing structure. Treat bundled examples as references, never automatic templates.
